; Raise upload limits for the student portal (profile photos, LMS attachments).
; Honoured by PHP-FPM / LiteSpeed / CGI when placed in the document root (public/).
; Changes can take a few minutes to apply (user_ini cache).
upload_max_filesize = 8M
post_max_size = 10M
